In Python, I strongly prefer Click as a Command Line Interface (CLI) library. I want to stucture my PHP code using a similar approach, where each command is a separate class and the CLI entry point simply dispatches to the appropriate command class.
