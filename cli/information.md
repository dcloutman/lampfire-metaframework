# Command Line Client
This should be a command-line tool with the following subcommands:
- init - Initializes the application with the application environment variables. If no .env file is found, it should ask the user if they want to intialize the application programmatically.
- The `.env` file should be at the project root. The `env_example` file is a starter for the userland user who forks the repository.

