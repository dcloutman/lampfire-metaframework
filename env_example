DATABASE_NAME='lampfire_dev'
DATABASE_HOST='db'
DATABASE_PORT='3306'
DATABASE_APPLICATION_USER='lampfire'
DATABASE_APPLICATION_USER_PASSWORD=''

# Root password for the MariaDB container. Used only by Docker.
MARIADB_ROOT_PASSWORD=''

APPLICATION_URL='http://localhost:8080'

# Set to 'true' to display detailed error messages.
APP_DEBUG='false'

# A 64-character hex string representing 256 bits of entropy.
# Generate with: php -r "echo bin2hex(random_bytes(32));"
PASETO_KEY=''
